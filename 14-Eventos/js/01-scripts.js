// Eventos

// Evento: DOMContentLoaded --> El evento se produce cuando el DOM se termina de cargar.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Documento listo');
    alert('Página cargada');
})