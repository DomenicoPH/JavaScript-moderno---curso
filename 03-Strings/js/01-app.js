// strings

const emptyString = '';

//Inicialización de variable = string (1)
const producto = 'Monitor 20 pulgadas'
console.log(producto);
        // Crea un STRING

//Inicialización de variable = string (2)
const producto2 = String('Monitor 24 pulgadas');
console.log(producto2);
        // Crea un STRING

//Inicialización de variable = string (3)
const producto3 = new String('Monitor 27 pulgadas');
console.log(producto3);
        // Crea un OJETO con un STRING

// Escape de caracteres
const ejemplo_1 = '20 "'    //Se puede escapar usando un tipo distinto de comillas.
const ejemplo_2 = "20 \""   //Cuando no sea posible, se escapa con \(barra invertida).

console.log(ejemplo_1)
console.log(ejemplo_2)